# Depression estimation using audio with machine learning models

## Dataset
Use dataset_fetch.py script to fetch dataset. Will need to kaggle auth. In the end, it will be around 5-6GB.

## How tu run?
- First run dataset_fetch.py
- Then run cnn_lstm_approach.py for CNN+LSTM
- Or svr_approach.py for SVR